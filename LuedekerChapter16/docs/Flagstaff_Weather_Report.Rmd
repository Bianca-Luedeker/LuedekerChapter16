---
title: "Flagstaff_Weather_Report"
author: "Bianca"
date: "2023-11-30"
output: pdf_document
---

Create a new directory in your package called docs/. In that directory create a RMarkdown file that loads your package and uses the weather data to make a few graphs of weather phenomena over time.

Load the package.

```{r}
library(LuedekerChapter16)
```

A plot of the daily max temp
```{r}
ggplot(Flagstaff_Weather) +
  geom_line(aes(x=date, y=TMAX)) +
  labs(title="Daily Max Temp in Flagstaff",
       x="date", y="Temp")
```
